version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    # IMPORTANT - Please verify your build commands
    build:
      commands: 
        - npm run build
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: \amplify\backend\api\ReactNativeEmail\build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
